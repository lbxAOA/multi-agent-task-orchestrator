# 多智能体任务复盘模板

> 每个实际发生**委派或团队协作**的任务结束、阻塞或取消时，必须从此模板创建一份独立记录：`docs/retrospectives/YYYY-MM-DD-<简短-kebab-case-任务名>.md`。适用 A2A peer、`delegate_task` 子代理及其他多成员协作；Hermes 独自完成的小任务不强制记录，除非发生高风险失败或用户要求。同名时追加 `-2`、`-3`，不得覆盖历史记录。
>
> 只记录真实可核验证据。绝不记录密码、密钥、token、`.env` 内容或完整敏感输出。未执行的项目必须明确写为“未运行”并解释原因。

## 基本信息

- 日期：
- 状态：完成 / 部分完成 / 阻塞 / 取消
- 仓库/工作目录：
- Skill 版本：
- 任务目标：
- 风险等级：低 / 中 / 高
- 是否允许写入：是 / 否
- 是否允许 commit / push / PR / 部署：

## 任务契约

- 完成定义：
- 允许修改范围：
- 禁止修改范围：
- 验收命令：
- 并行策略：串行 / 只读并行 / 隔离 worktree 并行

## 团队调度记录

| 阶段 | 执行通道 | 负责人 | 任务类型 | A2A context_id（如有） | 实际结果 |
|---|---|---|---|---|---|
| 计划 | Hermes | Hermes | 计划 | 不适用 | |
| 工作包 | 直接执行 / delegate_task / A2A | | 写入 / 只读 | | |
| 审查 | 直接执行 / delegate_task / A2A | | 只读 | | |
| 验收 | Hermes | Hermes | 验证 | 不适用 | |

## 可核验证据

- 修改文件：
- `git diff` 审查结论：
- 实际执行命令与退出码：
  - `命令` → `退出码`：
- 未运行的验收项及原因：
- 独立审查发现：

## 复盘

### 有效之处

- 

### 问题与根因

- 现象：
- 证据：
- 根因：
- 影响：

### 候选规则

```text
触发条件：...
Hermes 动作：...
验收标准：...
```

- 是否达到进入 `SKILL.md` 的门槛：否 / 第二次发生 / 高风险一次 / 用户明确要求
- 下一次验证方式：
