---
name: multi-agent-task-orchestrator
description: "用 Hermes 拆解、委派并验收多智能体任务。"
version: 0.3.1
author: lbxAOA, Hermes Agent
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [orchestration, delegation, a2a, multi-agent, coding, planning]
    related_skills: [claude-code, codex, hermes-agent]
created_by: agent
---

# 多智能体任务总管

Hermes 是总管：先判断任务是否值得委派，再拆解为可验收的工作包，选择合适的执行者，控制依赖和写入边界，并对最终结果负责。A2A 的 `claude_code` / `codex`、Hermes `delegate_task` 子代理，以及 Hermes 自己直接执行，都是可选手段；**不得因为存在某个 Agent 就强制使用它，也不得为小任务制造无价值的团队流程。**

## When to Use

- 任务包含两个以上可独立验收的工作包，或需要实现与独立审查、研究与验证、设计与测试分工。
- 用户要求团队协作、任务拆解、并行推进、交叉审查或更可靠的交付。
- 任务规模、风险或等待时间使委派能带来明确收益。

不要用于：
- 单一、小型且 Hermes 可直接完成并验证的任务；直接执行。
- 无法形成可验证产物的开放讨论；除非用户明确要求分工。
- 涉及密码、密钥、付款、生产发布或不可逆外部操作；先确认范围，且不得传递秘密。

## 委派决策

先决定**是否**委派，再决定委派给谁：

| 情况 | 默认动作 |
|---|---|
| 小改动、单文件分析、一步可验证任务 | Hermes 直接完成 |
| 可并行的只读调研、独立审查或测试设计 | 使用 `delegate_task` 或可用 A2A peers 并行收集证据 |
| 大型实现、跨文件重构、需要专长的编码工作 | 选择最合适的 A2A peer 或 `delegate_task`；由 Hermes 集成 |
| 已配置且能力匹配的 `claude_code` / `codex` | 可作为执行或独立审查成员，非强制 |
| 高风险变更 | 一方实现，未参与实现的成员只读复核；Hermes 最终验收 |

选择依据是任务边界、能力匹配、并行收益、写入冲突风险和验收成本；不要基于固定的“Claude Code 总是实现、Codex 总是审查”偏见路由。

## 可用执行通道

1. **Hermes 直接执行**：小任务或无法安全拆分的任务。
2. **`delegate_task`**：Hermes 内部、短时、边界明确的并行子任务。传入完整上下文；子代理不是持久服务。
3. **A2A peer**：已配置且能力匹配的外部/本地对等 Agent。使用前以 `a2a_list` 确认可用；首次选用某 peer 时以 `a2a_discover` 确认能力。对多轮 A2A 工作保存 `context_id`，必要时用 `a2a_history` 恢复。

A2A peer 缺失、不可达或能力不匹配时，不要硬派或伪称已委派；改用合适的可用通道，或如实报告阻塞。任何通道都不得接收 API key、密码、令牌或 `.env` 原文。

## 角色与任务单

角色按任务临时指定：总管/集成（Hermes）、实现者、研究者、测试者、独立审查者。一个成员可以承担多个不冲突角色；没有收益时不必凑齐角色。

每个委派工作包必须写明：

- 目标和完成定义；工作目录与允许/禁止范围。
- 负责人、依赖关系、是否只读或允许写入。
- 验收命令、交付格式和不确定项的报告要求。
- 是否允许 commit、push、PR、部署；未明确授权时一律不允许。

任务单模板：

```text
角色：...
目标：...
工作目录：...
范围：只读取/只修改 ...；不要触碰 ...。
依赖输入：...
验收：运行 ...，报告实际输出、退出码和未通过项。
交付：修改文件、证据、风险、建议的后续动作。
边界：不要处理密钥；不要提交、推送、发布，除非明确授权。
```

## 编排流程

### 1. 建立任务契约

先将用户目标整理为以下内容；复杂任务用 `todo` 跟踪：

- 目标、完成定义（Definition of Done）与不可变约束。
- 工作目录/仓库、目标分支和允许修改的文件范围。
- 验收命令、测试标准、是否允许提交、推送、创建 PR 或发布。
- 子任务依赖与共享文件风险。

信息不足且会影响实现或安全时，使用 `clarify` 一次性询问必要问题。低风险默认值要明确写进计划。

### 2. 设计可执行团队计划

将任务拆为有明确输入、输出和验收条件的工作包。每个工作包都必须包含：

- **负责人**：选定的 peer、子代理或 Hermes。
- **通道**：直接执行、`delegate_task` 或 A2A。
- **类型**：只读研究/审查，或会写入工作区的实现。
- **依赖**：它必须等待哪些结果。
- **交付格式**：修改的文件、命令及结果、遗留风险。
- **验收动作**：具体测试、diff 审查或可重复检查。

先把用户可见的简短计划说清楚；对于会扩展范围、改动架构、删除/迁移数据、提交/推送的工作，等用户批准后再开始。

### 3. 安全调度

- 只读任务可通过 `delegate_task` 或 A2A 并行派发；只有在证据、视角或等待时间上有明确收益时才这样做。
- **同一工作树中，绝不并行派发两个会写入同一或可能重叠文件集的任务。** 先完成一个、读取 diff/测试结果，再发送下一个。
- 需要真正并行写代码时，先由 Hermes 建立隔离 worktree/目录并在每张任务单中提供准确工作目录；之后才可并行分派。
- A2A 消息和 `delegate_task` 上下文都必须包含工作目录、边界、验收命令、允许的写入范围与提交限制。
- 对多轮 A2A 工作复用 `context_id`；对内部 `delegate_task`，将必要结果和证据显式汇总给后续成员。

### 4. 执行与反馈回路

1. 对选定通道执行委派：A2A 使用 `a2a_call` / `a2a_orchestrate`；内部任务使用 `delegate_task`；小任务由 Hermes 直接处理。
2. 对无写入冲突的独立评审/调研可并行收集互补结论；对写入任务按边界串行或隔离。
3. 记录每个工作包的执行通道、负责人、任务、A2A `context_id`（若有）、状态和实际结果；不要把“已发出”说成“已完成”。
4. 返回内容不足、测试失败或需要修复时，向同一成员补充任务，或交给独立成员复核；以证据而非回复长度解决冲突。
5. 如果成员要求超出授权的决定、秘密或高风险动作，暂停并向用户询问，不要替用户同意。

### 5. 可选复盘与规则演进

复盘不是默认动作，也不影响任务完成判定。只有在用户明确要求、任务契约明确启用，或用户提供历史复盘要求据此优化 Skill 时，Hermes 才创建复盘文件。发生重大故障时，Hermes 可以建议复盘，但未经明确启用不得自动创建。

1. 复盘启用后，从 `references/retrospective-template.md` 创建项目中的 `docs/retrospectives/YYYY-MM-DD-<简短-kebab-case-任务名>.md`。若同一天同名文件已存在，追加 `-2`、`-3` 等序号，不得覆盖旧记录。
2. 只填写可核验的真实信息：任务契约、工作目录、执行通道与实际负责人、A2A `context_id`（若有）、变更文件、`git diff` 结论、实际执行命令及退出状态、阻塞项与风险。
3. 没有执行某项验收时，明确填写“未运行”及原因；不得把成员的口头声称写成验证通过。
4. 保存复盘后，在最终报告中给出文件路径和一句结论。若复盘未启用，Hermes 不创建复盘文件，也不得声称已经生成。
5. 复盘中的候选规则只在满足下列任一门槛时才写回主 Skill：同类问题第二次出现；一次高风险事件；或用户明确要求修改团队行为。写回时更新版本、`CHANGELOG.md`，并在下一次匹配任务中验证。
6. 若项目未包含该目录或任务是临时、不可写工作区，且复盘已启用，则将复盘保存到总管 Skill 仓库；仍无法写入时，向用户明确报告该复盘未保存的原因。

### 6. 集成与验收

Hermes 必须亲自完成或真实调用工具验证：

1. 检查变更范围和 `git diff`，确保所有子任务结果可追溯。
2. 运行任务契约中定义的测试；必要时执行最小可复现 smoke test。
3. 将未通过测试、无法执行的命令、冲突和未验证假设如实报告。
4. 让未参与实现的 peer 对高影响改动进行只读审查；将明确问题反馈给主实现者修正并再次验证。
5. 只有在用户明确授权时，才提交、推送、创建 PR、部署或发布。

## 结果报告格式

最终报告必须简洁且基于真实结果：

- **团队与分工**：谁负责了什么。
- **完成内容**：可核对的文件/行为变更。
- **验证**：实际执行的命令及通过/失败状态。
- **未完成或风险**：没有掩盖的阻塞、测试缺口、后续建议。
- **协作连续性**：如后续工作需要，保留各执行通道的任务记录与 A2A `context_id`（若有）。

## 失败处理

- 委派通道不可达：报告通道、失败阶段和错误摘要；在边界允许时改用另一可用通道，否则报告阻塞。
- 一个成员失败：先确定是否可由另一成员或 Hermes 独立接手；避免将失败输出当作完成交付。
- 多成员结论冲突：比较证据、运行测试或请求针对性复核；不要简单按回复更长者采纳。
- 变更冲突：停止后续写入，保存当前 diff，选择一个集成负责人按顺序合并并验证。

## 验证清单

- [ ] 每个委派工作包都有边界、执行通道、目录和可检验验收条件。
- [ ] 并行写入使用了隔离工作区，或写入任务已串行执行。
- [ ] 若任务契约启用了复盘，复盘文件已保存并基于真实证据。
- [ ] 若未启用复盘，没有声称生成或保存复盘文件。
- [ ] 最终报告区分完成、失败、未验证与用户待决事项。
