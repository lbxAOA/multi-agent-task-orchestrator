---
name: a2a-code-team-orchestrator
description: "用 A2A 编排 Claude Code 与 Codex 的代码团队。"
version: 0.1.0
author: Hermes Agent
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [a2a, orchestration, claude-code, codex, coding, planning]
    related_skills: [claude-code, codex, hermes-agent]
created_by: agent
---

# A2A 代码团队总管

Hermes 是总管：负责澄清目标、拆解计划、分配角色、控制变更边界、汇总与验收。执行型成员通过 A2A 调用本机的 `claude_code` 和 `codex` 对等代理完成工作；不要把它们当作 Hermes 的 `delegate_task` 子代理，也不要在同一任务上绕过 A2A 直接启动 CLI。

## When to Use

- 用户要求由 Claude Code、Codex 或两者协作完成代码、审查、测试、重构、调研或交付。
- 任务需要明确的负责人、依赖关系、阶段性验收与最终整合。
- 需要让两个编码代理相互审查或交叉验证。

不要用于：
- 单一、很小的本地改动；直接由 Hermes 完成更高效。
- 没有可验证产物的纯开放式讨论；除非用户明确希望委派。
- 涉及密码、密钥、付款、生产发布或不可逆外部操作；先向用户确认范围，且不得传递秘密。

## 前置检查

1. 先用 `a2a_list` 确认已配置的 peers；预期名称为 `claude_code` 和/或 `codex`。
2. 首次使用每个 peer 时，用 `a2a_discover` 确认 Agent Card、能力与连接状态。
3. 若 peer 缺失、不可达或能力不匹配，清楚报告；不能伪称已经委派。
4. 不要在提示中传输 API key、密码、令牌或 `.env` 内容。

## 角色分工

默认按任务调整，而不是机械地每次双派：

| 角色 | 默认成员 | 职责 |
|---|---|---|
| 总管/集成/验收 | Hermes | 计划、任务单、依赖、最终测试、结果报告 |
| 主实现 | `claude_code` | 较大功能、跨文件改动、架构实现 |
| 独立审查/测试 | `codex` | 审查 diff、寻找回归、补充测试、替代实现 |
| 快速局部实现 | `codex` 或 `claude_code` | 选择当前任务最合适的 peer |
| 设计或风险复核 | 未写代码的另一 peer | 只读分析、方案比较、安全/测试审查 |

用户明确指定模型或角色时优先遵从。不要因“有两个代理”就制造没有价值的重复劳动。

## 编排流程

### 1. 建立任务契约

先将用户目标整理为以下内容；复杂任务用 `todo` 跟踪：

- 目标、完成定义（Definition of Done）与不可变约束。
- 工作目录/仓库、目标分支和允许修改的文件范围。
- 验收命令、测试标准、是否允许提交、推送、创建 PR 或发布。
- 子任务依赖与共享文件风险。

信息不足且会影响实现或安全时，使用 `clarify` 一次性询问必要问题。低风险默认值要明确写进计划。

### 2. 设计可执行团队计划

将任务拆为有明确输入、输出和验收条件的工作包。每个工作包都必须包含：

- **负责人**：`claude_code`、`codex` 或 Hermes。
- **类型**：只读研究/审查，或会写入工作区的实现。
- **依赖**：它必须等待哪些结果。
- **交付格式**：修改的文件、命令及结果、遗留风险。
- **验收动作**：具体测试、diff 审查或可重复检查。

先把用户可见的简短计划说清楚；对于会扩展范围、改动架构、删除/迁移数据、提交/推送的工作，等用户批准后再开始。

### 3. 安全调度

- 只读任务可以通过 `a2a_orchestrate(capability="code", mode="all")` 并行发送给多个 peer，或以单独 `a2a_call` 并发发出。
- **同一工作树中，绝不并行派发两个会写入同一或可能重叠文件集的任务。** 先完成一个、读取 diff/测试结果，再发送下一个。
- 需要真正并行写代码时，先由 Hermes 建立隔离 worktree/目录并在每张任务单中提供准确工作目录；之后才可并行分派。
- 每次 A2A 消息必须给出：工作目录、任务边界、验收命令、是否允许写文件，以及“不要提交/推送/发布，除非任务单明确允许”。
- 对已经分配的多轮任务，保存并复用返回的 `context_id`，通过 `a2a_call` 继续同一对话；用 `a2a_history` 在压缩或恢复后取回上下文。

推荐任务单模板：

```text
你是【角色】。在【绝对工作目录】完成以下工作：
目标：...
范围：只修改/只读取 ...；不要触碰 ...。
验收：运行 ...，报告实际输出和未通过项。
协作：不要提交、推送、发布或处理密钥；完成后列出修改文件、测试命令及结果、风险。
```

### 4. 执行与反馈回路

1. 对单个 peer 使用 `a2a_call(agent=..., message=...)`。
2. 对无写入冲突的独立评审/调研，可用 `a2a_orchestrate` 以 `all` 模式收集互补结论。
3. 记录每次调用的 peer、任务、`context_id`、状态和实际结果；不要把“已发出”说成“已完成”。
4. 返回内容不足、测试失败或需要修复时，用同一 `context_id` 追问具体缺口，或将可审查的结果交给另一 peer 复核。
5. 如果 agent 要求超出授权的决定、秘密或高风险动作，暂停并向用户询问，不要替用户同意。

### 5. 集成与验收

Hermes 必须亲自完成或真实调用工具验证：

1. 检查变更范围和 `git diff`，确保所有子任务结果可追溯。
2. 运行任务契约中定义的测试；必要时执行最小可复现 smoke test。
3. 将未通过测试、无法执行的命令、冲突和未验证假设如实报告。
4. 让未参与实现的 peer 对高影响改动进行只读审查；将明确问题反馈给主实现者修正并再次验证。
5. 只有在用户明确授权时，才提交、推送、创建 PR、部署或发布。

## 结果报告格式

最终报告必须简洁且基于真实结果：

- **团队与分工**：谁负责了什么。
- **完成内容**：可核对的文件/行为变更。
- **验证**：实际执行的命令及通过/失败状态。
- **未完成或风险**：没有掩盖的阻塞、测试缺口、后续建议。
- **A2A 连续性**：如后续工作需要，保留相关 `context_id` 供继续协作。

## 失败处理

- A2A 不可达：报告 peer 名称、失败阶段和错误摘要；可在用户同意下改用 Hermes 或等待服务恢复。
- 一个 peer 失败：先确定是否可由另一 peer 独立接手；避免将失败输出当作完成交付。
- 两个 peer 结论冲突：比较证据、运行测试或请求针对性复核；不要简单按回复更长者采纳。
- 变更冲突：停止后续写入，保存当前 diff，选择一个集成负责人按顺序合并并验证。

## 验证清单

- [ ] `a2a_list` 证实所用 peer 已配置。
- [ ] 每个 A2A 任务都有边界、目录和可检验验收条件。
- [ ] 并行写入使用了隔离工作区，或写入任务已串行执行。
- [ ] Hermes 已审查实际变更并运行/核验验收。
- [ ] 最终报告区分完成、失败、未验证与用户待决事项。
